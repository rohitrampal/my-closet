# FastAPI & server
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
python-multipart>=0.0.9,<1.0.0

# Database (async)
sqlalchemy[asyncio]>=2.0.36,<3.0.0
asyncpg>=0.30.0,<1.0.0
alembic>=1.14.0,<2.0.0

# Config & validation
pydantic[email]>=2.10.0,<3.0.0
email-validator>=2.0.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# Security (foundation only) — use bcrypt directly; passlib 1.7.x breaks on bcrypt>=4.1
bcrypt>=4.0.0,<5.0.0
python-jose[cryptography]>=3.3.0,<4.0.0

# Logging
python-json-logger>=3.2.0,<4.0.0

# Image tagging (download + Pillow)
httpx>=0.27.0,<1.0.0
pillow>=10.4.0,<12.0.0
huggingface_hub>=0.26.0,<1.0.0

# S3 uploads (clothes images)
boto3>=1.35.0,<2.0.0

# Gemini Vision (optional; garment analyze — falls back to Pillow/heuristics without key)
google-genai>=1.0.0,<2.0.0

# Outfit ranking (train offline; optional at runtime)
scikit-learn>=1.5.0,<2.0.0
numpy>=1.26.0,<3.0.0

# Razorpay premium checkout (2.x uses requests only; avoids legacy pkg_resources)
razorpay>=2.0.0,<3.0.0
